
```
# Install comprehensive monitoring
sudo apt install s-tui lm-sensors

# Run s-tui (terminal-based monitoring)
sudo s-tui

# Or for sensors
sensors
watch -n 0.5 'cat /sys/devices/system/cpu/cpu*/cpufreq/scaling_cur_freq'
```


```
# Install evtest to see keyboard events
sudo apt install evtest

# Monitor keyboard input latency
sudo evtest
# Choose your keyboard device, then type and watch timestamps
```


hello my son! assasdasdadaads

asda